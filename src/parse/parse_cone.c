#include "miniRT.h"

void parse_cone(char **tokens, t_scene *scene)
{
	int error;
	t_cone *new_cones;
    t_cone cone;

    error = 0;
    if (!tokens[1] || !tokens[2] || !tokens[3] || !tokens[4] || !tokens[5])
}
